[project]
name = "railway-zoomcamp-project"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "duckdb>=1.5.1",
    "google-cloud-storage>=3.10.1",
    "pandas>=3.0.1",
    "pyarrow>=23.0.1",
]
